<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Dashboard</title>
  </head>

  <body>
    <div class="container">
      <h2>Uploaded files</h2>

      <ul>
        <% files.forEach(f => { %>
          <li>
            <b><%= f.fileName %></b> (<%= f.category %>)
            <a href="/view/<%= f.id %>">View</a>
          </li>
        <% }) %>
      </ul>
    </div>
  </body>
</html>
